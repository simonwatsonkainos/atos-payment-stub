server:
  type: simple
  applicationContextPath: /
  adminContextPath: /admin
  connector:
    type: http
    port: 3002

soapServer:
  baseUrl: http://localhost:8888/
  paymentInitializePath: /services/v1/paymentInit
  # Enables additional logging to the console for any SOAP requests the server receives
  additionalSoapServerLogging: true

soapClient:
  baseUrl: http://localhost:5000/

redirectionBaseUrl: http://localhost:3002/payment/